Font & FontManager
====================
* Throw warnings when font not found
* Remove compile warnings

SGCTNetwork
===================
Add support for reading on server in order to create an server object that recieves messages from the clients.
Also add support for setting the port in the ReadConfig class via XML

char * tempBuffer = "What's up???\n";
int iSendResult = send( mNetwork->mSocket, tempBuffer, sizeof(tempBuffer), 0 );