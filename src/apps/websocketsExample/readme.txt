Download, build and install https://libwebsockets.org/. No SSL support needed (can be disabled in cmake gui).

Then open webbrowser and open http://localhost:9000/
Microsoft edge is not supported and causing errors in canvas renderings for hi-dpi monitors and touch drag not working.